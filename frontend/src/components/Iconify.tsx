"use client";

import "iconify-icon";

/**
 * Registers the <iconify-icon> web component once on the client.
 * Render it anywhere high in the tree (we mount it in the root layout).
 */
export default function Iconify() {
  return null;
}
